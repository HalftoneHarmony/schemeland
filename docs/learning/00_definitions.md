# 🏗️ 코딩 펀더멘탈 (Coding Fundamentals)

이 문서는 프로젝트를 진행하면서 마주치는 개발의 기초 개념과 용어들을 정리한 사전입니다.

---

## 1. 우리 동네 지도와 언어 (Project Structure & File Types)

코딩의 세계에는 다양한 파일 확장자(이름표)와 폴더들이 있습니다. 우리 앱을 구성하는 요소들을 알기 쉽게 정리해 드릴게요.

### 📁 폴더 지도 (Folder Map)

1.  **`src/` (진짜 재료 상자 - Source Code)**
    -   **프로젝트의 가장 중요한 폴더입니다!** 앱을 만드는 모든 실제 코드들이 들어있습니다.
    -   `components/`, `services/` 등이 모두 이 안에 모여 있습니다.
    -   💡 **팁**: 간혹 오타로 **`srt`**라고 부르기도 하는데, `srt`는 자막 파일의 확장자이고 코딩에서는 `src`가 맞습니다.

2.  **`components/` (레고 블록 상자)**
    -   화면을 구성하는 조각들이 모여 있습니다.
    -   `views/`: 큰 방들 (안방, 거실 등 화면 전체)
    -   버튼이나 카드 같은 작은 부품들도 여기에 있습니다.
    
3.  **`services/` (심부름 센터)**
    -   화면에는 보이지 않지만 뒤에서 중요한 일을 처리하는 일꾼들입니다.
    -   `geminiService.ts`: 구글 Gemini AI에게 일을 시키고 결과를 받아오는 역할을 합니다.

4.  **`node_modules/` (공구함)**
    -   `npm install`로 받아온 수만 가지의 라이브러리(남이 짠 코드)가 들어있습니다. 너무 복잡하니 절대 열어보지 마세요! (자동으로 관리됩니다)

5.  **`docs/` (도서관)**
    -   지금 읽고 계신 이 파일처럼, 문서와 기록을 보관하는 곳입니다.

### 📝 파일 이름표 (File Extensions)

1.  **`.ts` (TypeScript)**: **"논리책"**
    -   화면 그림 없이 **오직 로직(계산, 함수, 데이터 처리)**만 들어있는 파일입니다.
    -   예: `geminiService.ts` (AI랑 통신하는 로직만 있음)

2.  **`.tsx` (TypeScript + React)**: **"그림책"**
    -   `.ts`와 비슷하지만, **화면을 그리는 코드(HTML 같은 것)**가 포함되어 있습니다.
    -   `x`는 XML(태그)의 약자라고 생각하면 쉽습니다.
    -   예: `App.tsx`, `LandingView.tsx` (눈에 보이는 화면들)

3.  **`.json` (JavaScript Object Notation)**: **"설정 파일 / 장부"**
    -   데이터를 있는 그대로 저장하는 텍스트 파일입니다.
    -   코드(행동)는 없고, 데이터(정보)만 있습니다.
    -   예: `package.json` (이 프로젝트의 이름, 버전, 필요한 라이브러리 목록이 적힌 명세서)

4.  **`.css` (Cascading Style Sheets)**: **"옷장 (스타일)"**
    -   화면을 예쁘게 꾸미는(색깔, 크기, 배치) 규칙들이 적혀 있습니다.

---

## 2. 리액트 핵심 개념 (React Core Concepts)

### 🧱 컴포넌트 (Component) = "레고 블록"
-   **정의**: 화면을 구성하는 독립적인 조각입니다.
-   **설명**: 버튼 하나, 입력창 하나, 그리고 그것들을 모은 화면 전체도 모두 컴포넌트입니다.
-   **장점**: 한 번 만들어둔 '버튼' 블록은 거실(페이지)에서도 쓰고 안방(페이지)에서도 쓸 수 있습니다(재사용).

### 🎁 프롭스 (Props) = "부모님이 주신 선물"
-   **정의**: 부모 컴포넌트가 자식 컴포넌트에게 전달하는 데이터입니다.
-   **특징**: 자식은 이걸 받아서 쓰기만 할 뿐, 바꿀 수는 없습니다.
-   **예시**: "이 버튼의 색깔은 파란색으로 해라"라는 명령을 Props로 전달하면, 버튼 컴포넌트는 파란색으로 그려집니다.

### 🧠 스테이트 (State) = "나의 기억/상태"
-   **정의**: 컴포넌트가 가지고 있는, 변할 수 있는 데이터입니다.
-   **특징**: 이게 바뀌면 화면이 자동으로 다시 그려집니다(리렌더링).
-   **예시**: 
    -   `count`: 카운터 숫자 (1 -> 2 -> 3)
    -   `text`: 입력창에 내가 지금 쓰고 있는 글자들
    -   `isOpen`: 메뉴가 열려있는지 닫혀있는지(True/False)

---

## 3. 웹 저장소 (Web Storage)

### 🗄️ 로컬 스토리지 (Local Storage) = "개인 사물함"
-   **정의**: 브라우저 안에 저장되는 반영구적인 저장소입니다.
-   **특징**: 새로고침하거나 브라우저를 껐다 켜도 데이터가 살아있습니다.
-   **위치**: 개발자 도구 (F12) -> Application 탭 -> Local Storage
